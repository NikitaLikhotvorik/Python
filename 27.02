lp = True
def setup():
    size(600,600)
    background(0)
def draw():
     circle(random(0,600),random(0,600),random(5,15))
def mouseClicked():
    global lp
    if lp:
        lp = False
        noLoop()
    else:
        lp = True
        loop()
