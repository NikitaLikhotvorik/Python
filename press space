lp = True
def setup():
    size(600,600)
    background(0)
def draw():
    square(random(0,600),random(0,600),random(5,15))
    if keyPressed:
        if key == ' ':
           fill(random(0,255),random(0,255),random(0,255))
